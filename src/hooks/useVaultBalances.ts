import useVaults from "./useVaults";

const useVaultBalances = () => {
    const { vaults } = useVaults();

    // const getAllVaultBalances = async () => {
    // multicall balanceof vaults
    // react query

    // return balances
    return {};
};

export default useVaultBalances;
